import ParentDiv from "@/components/test";
import React from "react";

interface Props {}

const Page = (props: Props) => {
  return (
    <div>
      <ParentDiv />
    </div>
  );
};

export default Page;
