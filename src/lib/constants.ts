export const gap = 2 / 5; // the value is "0.5 rem" get this value form the tailwind css "spacing -> pa"
/* suppose line height is 1.75rem and row-gap is 0.5rem so if line height is x then gap is x * 0.5/1.75 = x * 2 / 7
    and we are calculating gap in terms of height because we calculate height in the function
    findHeightWidth.ts


    for line height = 2rem and gap is 0.8rem
    0.8/2
*/
